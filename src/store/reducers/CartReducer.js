const initialState = {
  products: [],
  totalPrice: 0,
  totalQuantity: 0,
};

const CartReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default CartReducer;
